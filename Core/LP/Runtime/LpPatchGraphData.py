class LpPatchGraphData:
    def __init__(self, x_lim, y_lim, polygons_datas, odr_points, lines, legend_handles, polygons_points, title):
        self.x_lim = x_lim
        self.y_lim = y_lim
        self.polygons_datas = polygons_datas
        self.odr_points = odr_points
        self.lines = lines
        self.legend_handles = legend_handles
        self.polygons_points = polygons_points
        self.title = title
